import 'package:flutter/material.dart';

class ThierdScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
          child:Text("Third Screen",style: TextStyle(color: Colors.black,fontSize: 18),)
      ),
      height: double.infinity,
      width: double.infinity,
    );
  }
}
